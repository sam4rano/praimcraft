import { defineConfig } from 'sanity'

export default defineConfig({
  name: 'praimcraft',
  title: 'Praimcraft CMS',
  projectId: 'a9x04if7',
  dataset: 'production',
  basePath: '/studio',
  plugins: [],
  schema: {
    types: [],
  },
})
